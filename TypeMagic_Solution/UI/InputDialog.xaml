<Window x:Class="TypeMagic.UI.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ввод данных" 
        Width="400" Height="200"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#FFFFFF"
        Topmost="True">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="txtPrompt" Text="Введите значение:" 
                   FontFamily="Segoe UI" FontSize="12" Margin="0,0,0,10"/>

        <TextBox Grid.Row="1" x:Name="txtInput" 
                 FontFamily="Segoe UI" FontSize="12" 
                 Height="25" VerticalAlignment="Top" Text=""/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="OK" Width="80" Height="30" Margin="5" 
                    IsDefault="True" Click="BtnOk_Click"/>
            <Button Content="Отмена" Width="80" Height="30" Margin="5" 
                    IsCancel="True" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
